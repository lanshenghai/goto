{
  "name": "goto",
  "displayName": "goto",
  "description": "",
  "version": "0.0.3",
  "engines": {
    "vscode": "^1.96.0"
  },
  "categories": [
    "Other"
  ],
  "activationEvents": [
    "onCommand:goto.helloWorld",
    "onCommand:goto.compare",
    "onCommand:goto.compareWithClipboard",
    "onCommand:goto.lastCompare"
  ],
  "main": "./extension.js",
  "contributes": {
    "commands": [
      {
        "command": "goto.helloWorld",
        "title": "Goto"
      },
      {
        "command": "goto.compare",
        "title": "Python Variable Compare"
      },
      {
        "command": "goto.compareWithClipboard",
        "title": "Python Variable Compare With Clipboard"
      },
      {
        "command": "goto.lastCompare",
        "title": "Python Variable Compare With Last Comparison"
      }
    ]
  },
  "scripts": {
    "lint": "eslint .",
    "pretest": "yarn run lint",
    "test": "vscode-test",
    "package": "vsce package"
  },
  "devDependencies": {
    "@types/mocha": "^10.0.10",
    "@types/node": "20.x",
    "@types/vscode": "^1.96.0",
    "@vscode/test-cli": "^0.0.10",
    "@vscode/test-electron": "^2.4.1",
    "eslint": "^9.16.0"
  },
  "dependencies" : {
    "diff": "^8.0.2"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/lanshenghai/goto.git"
  }
}
